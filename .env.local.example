# Integration tests: paths so single-file runs work without re-running global setup.
# Bun loads .env.local automatically when you run `bun test`.
#
# 1. Run integration global setup once:
#      pnpm test:integration
#    (or: bun run scripts/run-integration-global-setup.ts)
# 2. Copy the paths from the files written under .taskgraph/ into the values below.
# 3. Copy this file to .env.local and fill in:
#      cp .env.local.example .env.local
#
# Then you can run individual integration tests, e.g.:
#      bun test __tests__/integration/setup-scaffold.test.ts
#
DOLT_ROOT_PATH=
TG_GOLDEN_TEMPLATE=
